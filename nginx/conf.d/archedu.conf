server {
    listen 80;
    server_name archedu.uz www.archedu.uz;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    location / {
        return 404;
    }
}
